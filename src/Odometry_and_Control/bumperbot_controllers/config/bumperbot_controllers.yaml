controller_manager:
  ros__parameters:
    update_rate: 100
    use_sim_time: true
    
    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster
      
    simple_velocity_controller:
      type: velocity_controllers/JointGroupVelocityController
    
    bumperbot_controller:
      type: diff_drive_controller/DiffDriveController

simple_velocity_controller:
  ros__parameters:
    joints:
      - wheel_left_joint
      - wheel_right_joint

#parameters for pre-made diffdrive controller
bumperbot_controller:
  ros__parameters:
    type        : diff_drive_controller/DiffDriveController
    use_stamped_vel: false
    left_wheel_names  : ['wheel_left_joint']
    right_wheel_names : ['wheel_right_joint']

    publish_rate: 100.0               # default: 50
    pose_covariance_diagonal : [0.001, 0.001, 1.0e-3, 1.0e-3, 1.0e-3, 0.01]
    twist_covariance_diagonal: [0.001, 0.001, 1.0e-3, 1.0e-3, 1.0e-3, 0.01]

    wheel_radius : 0.034
    wheel_separation : 0.174

    wheel_separation_multiplier: 1.0
    left_wheel_radius_multiplier: 1.0
    right_wheel_radius_multiplier: 1.0

    # Velocity commands timeout [s], default 0.5
    # cmd_vel_timeout: 0.5

    # Base frame_id
    base_frame_id: base_footprint #default: base_link

    # Publish limited velocity
    publish_limited_velocity: true

    # Publish wheel data
    publish_wheel_data: true

    # Publish to TF directly or not
    enable_odom_tf: true

    # Velocity and acceleration limits
    # Whenever a min_* is unspecified, default to -max_*
    linear:
      x:
        has_velocity_limits    : true
        max_velocity           : 2.0  # m/s
        min_velocity           : -2.0 # m/s
    angular:
      z:
        has_velocity_limits    : true
        max_velocity           : 3.14  # rad/s

# this node parameters are for diff_drive_kinematics(convert /cmd_vel to wheel velocity commands)
diff_drive_kinematics:
  ros__parameters:
    wheel_radius: 0.034
    wheel_separation: 0.174
    use_sim_time: true

#this node adds nose to odometry to simulate almst real-world imperfections.
adding_noise_to_odometry:
  ros__parameters:
    wheel_radius: 0.034
    wheel_separation: 0.174
    noise_factor: 0.005